create a plan for onboarding for User at every level. I have to build it.  from frontend to backend every thing. how it works how invite send to at every user level.  I think Tenency level can be we are at platform level (we are ) -> Org (superadmin) -> Tenent(tenent_admin) -> call Centers(cc_admin, agents, supervisor etc). I cannot be correct also tell mw what do you suggest and also reseacr it how comappitar doing it.
